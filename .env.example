# Nhost Configuration
# --------------------------------------------------
# Nhost project subdomain (required)
# Get this from your Nhost project dashboard: https://app.nhost.io
# Local development: use "local" for Nhost CLI
# Staging/Production: use your actual project subdomain
NEXT_PUBLIC_NHOST_SUBDOMAIN=local

# Nhost region (optional for local, required for cloud)
# Available regions: eu-central-1, us-east-1, ap-southeast-1, etc.
# Leave empty for local development
# Example for staging: eu-central-1
NEXT_PUBLIC_NHOST_REGION=

# Nhost Admin Secret (required for GraphQL codegen)
# Get this from Nhost dashboard > Settings > Environment Variables
# SECURITY: Never commit this value to version control
# This is only needed for development and code generation
NHOST_ADMIN_SECRET=your-admin-secret-here

# Application URL Configuration
# --------------------------------------------------
# Public URL where the application is hosted
# Used for email verification and password reset redirects
# Local development: http://localhost:3000
# Staging: https://staging.yourdomain.com
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# reCAPTCHA Configuration
# --------------------------------------------------
# Google reCAPTCHA v3 for rate limiting and bot protection
# Get keys from: https://www.google.com/recaptcha/admin

# reCAPTCHA Site Key (public, used in frontend)
# This key is visible to users in the browser
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your-site-key-here

# reCAPTCHA Secret Key (private, used in backend/API routes)
# SECURITY: Never expose this in client-side code
RECAPTCHA_SECRET_KEY=your-secret-key-here

# Setup Instructions
# --------------------------------------------------
# 1. Copy this file to .env.local for local development
# 2. Copy this file to .env.staging for staging environment
# 3. Replace all placeholder values with actual credentials
# 4. Never commit .env.local or .env.staging to version control
#
# For local development with Nhost CLI:
#   - Set NEXT_PUBLIC_NHOST_SUBDOMAIN=local
#   - Leave NEXT_PUBLIC_NHOST_REGION empty
#   - Run: nhost dev
#
# For staging/production:
#   - Set NEXT_PUBLIC_NHOST_SUBDOMAIN to your project subdomain
#   - Set NEXT_PUBLIC_NHOST_REGION to your project region
#   - Get credentials from Nhost dashboard
#
# For reCAPTCHA setup:
#   1. Go to https://www.google.com/recaptcha/admin
#   2. Register your site with reCAPTCHA v3
#   3. Add your domains (localhost for dev, your domain for prod)
#   4. Copy the Site Key to NEXT_PUBLIC_RECAPTCHA_SITE_KEY
#   5. Copy the Secret Key to RECAPTCHA_SECRET_KEY
