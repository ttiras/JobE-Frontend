# NavigationButtons - Visual Design Guide

**Component**: `components/evaluation/NavigationButtons.tsx`  
**Status**: ✅ Complete

---

## Desktop Layout (≥640px)

### Normal State (All Enabled)
```
┌─────────────────────────────────────────────────────────────┐
│ ─────────────────────────────────────────────────────────── │ ← border-top
│ ↕ 24px (pt-6)                                               │
│                                                             │
│  ┌──────────────┐                 ┌────────────┐  ┌──────────────────┐
│  │ < Previous   │                 │ 💾 Save   │  │ Save & Next  >   │
│  │  (outline)   │                 │   Draft   │  │   (default)      │
│  └──────────────┘                 │  (ghost)  │  └──────────────────┘
│                                   └────────────┘
│  ← Left aligned                    Right aligned (gap-2) →
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### First Dimension (Previous Disabled)
```
┌─────────────────────────────────────────────────────────────┐
│ ─────────────────────────────────────────────────────────── │
│                                                             │
│  ┌──────────────┐                 ┌────────────┐  ┌──────────────────┐
│  │ < Previous   │                 │ 💾 Save   │  │ Save & Next  >   │
│  │ [DISABLED]   │                 │   Draft   │  │   (enabled)      │
│  │ 50% opacity  │                 └────────────┘  └──────────────────┘
│  └──────────────┘
│   Tooltip: "First Dimension"
└─────────────────────────────────────────────────────────────┘
```

### No Selection (Next Disabled)
```
┌─────────────────────────────────────────────────────────────┐
│ ─────────────────────────────────────────────────────────── │
│                                                             │
│  ┌──────────────┐                 ┌────────────┐  ┌──────────────────┐
│  │ < Previous   │                 │ 💾 Save   │  │ Save & Next  >   │
│  │  (enabled)   │                 │   Draft   │  │  [DISABLED]      │
│  └──────────────┘                 └────────────┘  │  50% opacity     │
│                                                   └──────────────────┘
│                                    Tooltip: "Please select a level"
└─────────────────────────────────────────────────────────────┘
```

### Loading State (Saving)
```
┌─────────────────────────────────────────────────────────────┐
│ ─────────────────────────────────────────────────────────── │
│                                                             │
│  ┌──────────────┐                 ┌────────────┐  ┌──────────────────┐
│  │ < Previous   │                 │ 💾 Save   │  │ ⟳ Saving...      │
│  │  (enabled)   │                 │   Draft   │  │  [DISABLED]      │
│  └──────────────┘                 └────────────┘  │  (spinning icon) │
│                                                   └──────────────────┘
└─────────────────────────────────────────────────────────────┘
```

### Last Dimension (Finalize Text)
```
┌─────────────────────────────────────────────────────────────┐
│ ─────────────────────────────────────────────────────────── │
│                                                             │
│  ┌──────────────┐                 ┌────────────┐  ┌──────────────────┐
│  │ < Previous   │                 │ 💾 Save   │  │   Finalize  >    │
│  │  (enabled)   │                 │   Draft   │  │   (enabled)      │
│  └──────────────┘                 └────────────┘  └──────────────────┘
│                                                             │
│                                    Text changed: "Finalize"
└─────────────────────────────────────────────────────────────┘
```

### Without Save Draft (Optional Hidden)
```
┌─────────────────────────────────────────────────────────────┐
│ ─────────────────────────────────────────────────────────── │
│                                                             │
│  ┌──────────────┐                            ┌──────────────────┐
│  │ < Previous   │                            │ Save & Next  >   │
│  │  (outline)   │                            │   (default)      │
│  └──────────────┘                            └──────────────────┘
│                                                             │
│  ← Left aligned                       Right aligned →
└─────────────────────────────────────────────────────────────┘
```

---

## Mobile Layout (<640px)

### All Buttons Stacked
```
┌────────────────────────┐
│ ────────────────────── │ ← border-top
│ ↕ 16px (gap-4)        │
│                        │
│ ┌────────────────────┐ │ ← Full width (w-full)
│ │   < Previous       │ │
│ │    (outline)       │ │
│ └────────────────────┘ │
│                        │
│ ↕ 16px (gap-4)        │
│                        │
│ ┌────────────────────┐ │
│ │   💾 Save Draft    │ │
│ │    (ghost)         │ │
│ └────────────────────┘ │
│                        │
│ ↕ 16px (gap-4)        │
│                        │
│ ┌────────────────────┐ │
│ │  Save & Next  >    │ │
│ │    (default)       │ │
│ └────────────────────┘ │
│                        │
└────────────────────────┘
```

### Mobile - Loading State
```
┌────────────────────────┐
│ ────────────────────── │
│                        │
│ ┌────────────────────┐ │
│ │   < Previous       │ │
│ └────────────────────┘ │
│                        │
│ ┌────────────────────┐ │
│ │   💾 Save Draft    │ │
│ └────────────────────┘ │
│                        │
│ ┌────────────────────┐ │
│ │  ⟳ Saving...       │ │
│ │   [DISABLED]       │ │
│ └────────────────────┘ │
│                        │
└────────────────────────┘
```

### Mobile - Without Save Draft
```
┌────────────────────────┐
│ ────────────────────── │
│                        │
│ ┌────────────────────┐ │
│ │   < Previous       │ │
│ └────────────────────┘ │
│                        │
│ ↕ 16px (gap-4)        │
│                        │
│ ┌────────────────────┐ │
│ │  Save & Next  >    │ │
│ └────────────────────┘ │
│                        │
└────────────────────────┘
```

---

## Button Anatomy

### Previous Button (Outline)
```
┌─────────────────────┐
│ [2px border-outline]│
│                     │
│  < ← Previous       │
│  ↑   ↑             │
│  │   └─ Text       │
│  └─ ChevronLeft    │
│     (16x16px)      │
│                     │
└─────────────────────┘
    8px margin-right
```

### Save & Next Button (Default)
```
┌─────────────────────┐
│ [Primary bg color]  │
│                     │
│  Save & Next  → >   │
│       ↑       ↑     │
│       │       └─ ChevronRight
│       │          (16x16px)
│       └─ Text    │
│                     │
└─────────────────────┘
    8px margin-left
```

### Save Draft Button (Ghost)
```
┌─────────────────────┐
│ [No bg/border]      │
│ (hover shows bg)    │
│                     │
│  💾 → Save Draft    │
│  ↑    ↑            │
│  │    └─ Text      │
│  └─ Save Icon      │
│     (16x16px)      │
│                     │
└─────────────────────┘
    8px margin-right
```

### Loading Button State
```
┌─────────────────────┐
│ [Primary bg]        │
│ [50% opacity]       │
│                     │
│  ⟳ → Saving...      │
│  ↑    ↑            │
│  │    └─ Text      │
│  └─ Loader2        │
│     (spinning)     │
│     (16x16px)      │
│                     │
└─────────────────────┘
    animate-spin
```

---

## Icon Specifications

### ChevronLeft Icon
```
Size: 16x16px (h-4 w-4)
Position: Before text (mr-2 = 8px margin)
Color: Inherits button text color
Stroke width: 2px (default)
```

### ChevronRight Icon
```
Size: 16x16px (h-4 w-4)
Position: After text (ml-2 = 8px margin)
Color: Inherits button text color
Stroke width: 2px (default)
```

### Save Icon
```
Size: 16x16px (h-4 w-4)
Position: Before text (mr-2 = 8px margin)
Color: Inherits button text color
Stroke width: 2px (default)
```

### Loader2 Icon (Loading)
```
Size: 16x16px (h-4 w-4)
Position: Before text (mr-2 = 8px margin)
Color: Inherits button text color
Animation: spin (1s linear infinite)
Rotation: 360° continuous
```

---

## Spacing Breakdown

### Container Spacing
```
Desktop:
┌─────────────────────────────────────┐
│ pt-6 (24px)                         │
│ ↕                                   │
│ [Previous] ← → [Save] [Next]        │
│            justify-between          │
└─────────────────────────────────────┘

Mobile:
┌───────────────┐
│ pt-6 (24px)   │
│ ↕             │
│ [Previous]    │
│ ↕ gap-4 (16px)│
│ [Save]        │
│ ↕ gap-4 (16px)│
│ [Next]        │
└───────────────┘
```

### Button Group Spacing (Right Side)
```
Desktop:
┌────────────────────────┐
│ [Save Draft] [Next]    │
│ ← gap-2 (8px) →        │
└────────────────────────┘

Mobile:
┌────────────────┐
│ [Save Draft]   │
│ ↕ gap-4 (16px) │
│ [Next]         │
└────────────────┘
```

---

## State Transitions

### Button States Flow
```
Normal State:
  Enabled → Hover → Active → Normal
  
Disabled State:
  Normal (50% opacity, cursor-not-allowed)
  
Loading State:
  Normal → Loading (spinner) → Success/Error
```

### Text Changes
```
Normal → "Save & Next"
Last Dimension → "Finalize"
Loading → "Saving..."
```

---

## Tooltip Positioning

### Previous Button Tooltip (Disabled)
```
┌──────────────────────┐
│ First Dimension      │ ← Tooltip
└──────────┬───────────┘
           │
    ┌──────┴────────┐
    │ < Previous    │
    │  [DISABLED]   │
    └───────────────┘
```

### Next Button Tooltip (Disabled)
```
┌────────────────────────────────────┐
│ Please select a level to continue  │ ← Tooltip
└──────────────┬─────────────────────┘
               │
        ┌──────┴──────────┐
        │ Save & Next  >  │
        │   [DISABLED]    │
        └─────────────────┘
```

---

## Button Variants Comparison

### Outline vs Default vs Ghost
```
Outline (Previous):
┌──────────────┐
│ ╔══════════╗ │ ← Border (2px)
│ ║          ║ │
│ ║ Previous ║ │ ← Transparent background
│ ║          ║ │
│ ╚══════════╝ │
└──────────────┘

Default (Save & Next):
┌──────────────┐
│ ████████████ │ ← Primary background
│ █          █ │
│ █ Save & █ █ │ ← White text
│ █  Next  █ █ │
│ ████████████ │
└──────────────┘

Ghost (Save Draft):
┌──────────────┐
│              │ ← No border/background
│   Save Draft │ ← Text only
│              │ ← Hover shows background
└──────────────┘
```

---

## Responsive Breakpoint Behavior

### At 640px Transition
```
< 640px (Mobile):
flex-col                    ← Stack vertically
gap-4                       ← 16px between all items
w-full on all buttons       ← Full width buttons

≥ 640px (Desktop):
sm:flex-row                 ← Horizontal layout
sm:justify-between          ← Space between left/right
sm:items-center             ← Vertical center alignment
sm:w-auto on buttons        ← Content-based width
gap-2 on right group        ← 8px between Save/Next
```

---

## Color Scheme

### Previous Button (Outline)
```
Normal:
  Border: border-input (#e5e7eb)
  Text: foreground color
  Background: transparent

Hover:
  Background: accent (#f9fafb)
  Border: accent-foreground
  
Disabled:
  Opacity: 50%
  Cursor: not-allowed
```

### Save & Next Button (Default)
```
Normal:
  Background: primary color
  Text: primary-foreground (white)
  Border: none
  
Hover:
  Background: primary (darker shade)
  
Disabled:
  Opacity: 50%
  Cursor: default
  
Loading:
  Opacity: 50%
  Spinner: white color
```

### Save Draft Button (Ghost)
```
Normal:
  Background: transparent
  Text: foreground color
  Border: none
  
Hover:
  Background: accent
  Text: accent-foreground
```

---

## Animation Details

### Loader Spin Animation
```css
@keyframes spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}

.animate-spin {
  animation: spin 1s linear infinite;
}
```

**Visual**:
```
Frame 1:  ⟳  (0°)
Frame 2:  ⟲  (90°)
Frame 3:  ⟳  (180°)
Frame 4:  ⟲  (270°)
Frame 5:  ⟳  (360° = 0°)
... continues infinitely
```

---

## Complete Layout with Context

### Full Evaluation Page Layout
```
┌─────────────────────────────────────────────────┐
│ Header (Progress: 2/5)                          │
├─────────────────────────────────────────────────┤
│ Dimension Question Card                         │
│                                                 │
│ Technical Skills [✓ S3 Selected]                │
│ [D1] [Weight: 33%] [Max: S5]                   │
│                                                 │
│ ┌─────────────────────────────────────────────┐ │
│ │ 🟢 S1  Basic skills                         │ │
│ └─────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────┐ │
│ │ 🟡 S2  Intermediate skills                  │ │
│ └─────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────┐ │
│ │ 🟡 S3  Advanced skills               ✓     │ │
│ └─────────────────────────────────────────────┘ │
│                                                 │
│ ─────────────────────────────────────────────── │ ← border-top
│                                                 │
│ [< Previous]        [Save Draft] [Save & Next >] │ ← NavigationButtons
│                                                 │
└─────────────────────────────────────────────────┘
```

---

**Visual Design Complete**: ✅  
**All States Documented**: ✅  
**Responsive Specs**: ✅  
**Ready for Integration**: ✅
